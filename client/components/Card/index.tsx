import Link from "next/link";

import classes from './index.module.scss';

const Card = () => {
  return (
    <div className={`${classes.card} ${classes.w100}`}>
      
    </div>
  )
}

export default Card;